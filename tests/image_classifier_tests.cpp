/*
//
// Created by Omar on 10/20/2020.
//
#include <core/image_classifier.h>

#include <catch2/catch.hpp>

TEST_CASE("Image Classifier") {
  SECTION("Determine if 0 was parsed correctly") {
    REQUIRE(true);
  }

  SECTION("Determine if 1 was parsed correctly") {
    REQUIRE(true);
  }

  SECTION("Determine if 2 was parsed correctly") {
    REQUIRE(true);
  }

  SECTION("Determine if 3 was parsed correctly") {
    REQUIRE(true);
  }

  SECTION("Determine if 4 was parsed correctly") {
    REQUIRE(true);
  }

  SECTION("Determine if 5 was parsed correctly") {
    REQUIRE(true);
  }

  SECTION("Determine if 6 was parsed correctly") {
    REQUIRE(true);
  }

  SECTION("Determine if 7 was parsed correctly") {
    REQUIRE(true);
  }

  SECTION("Determine if 8 was parsed correctly") {
    REQUIRE(true);
  }

  SECTION("Determine if 9 was parsed correctly") {
    REQUIRE(true);
  }

  SECTION("Check if accuracy was greater than 70%") {
    REQUIRE(true);
  }

  SECTION("Check if pixel probabilities was correct") {
    REQUIRE(true);
  }

  SECTION("Determine if likelihood scores were correct") {
    REQUIRE(true);
  }
}
 */
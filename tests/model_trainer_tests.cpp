//
// Created by Omar on 10/20/2020.
//
#include <core/model_trainer.h>

#include <catch2/catch.hpp>

TEST_CASE("Model Training") {
  SECTION("Check to see if model was trained correctly") {
    REQUIRE(true);
  }

  SECTION("Check probability for each image") {
    REQUIRE(true);
  }

  SECTION("Check the occurence of each class") {
    REQUIRE(true);
  }

  SECTION("Check if a given pixel is shaded") {
    REQUIRE(true);
  }

  SECTION("Check if a given pixel is unshaded") {
    REQUIRE(true);
  }

  SECTION("Check class probability") {
    REQUIRE(true);
  }
}

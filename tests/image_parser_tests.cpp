//
// Created by Omar on 10/13/2020.
//

#include <core/image_parser.h>

#include <catch2/catch.hpp>

TEST_CASE("Image Parsing") {
  SECTION("Correct Number of Images Parsed")
  {
    REQUIRE(true);
  }

  SECTION("Check that the number of variances within a given number is correct")
  {
    REQUIRE(true);
  }

  SECTION("Check that labels are correctly correlated to each image")
  {
    REQUIRE(true);
  }

  SECTION("Check that operator overloading correctly parses images from file")
  {
    REQUIRE(true);
  }

  SECTION("Check that improper file paths do no result in image parsing")
  {
    REQUIRE(true);
  }

  SECTION("Check that files that do not exist does not result in image parsing")
  {
    REQUIRE(true);
  }
}